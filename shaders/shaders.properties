#===================================================================
# Lumen — shaders.properties
# Pipeline config + options screen, grouped by domain (docs/01 sec 6).
#===================================================================

# --- Buffer sizes (docs/01 section 3) ---
size.buffer.colortex4 = 0.5 0.5
size.buffer.colortex6 = 0.5 0.5

# --- Options screen: domain groups ---
screen = [Lighting] [Water] [ColorExposure] [Performance]
screen.columns = 2

screen.Lighting = SUN_INTENSITY TORCH_INTENSITY
screen.Water = WAVE_HEIGHT REFRACT_STRENGTH
screen.ColorExposure = EXPOSURE_COMP
screen.Performance = QUALITY_TIER DEBUG_BUFFER

sliders = QUALITY_TIER DEBUG_BUFFER SUN_INTENSITY TORCH_INTENSITY EXPOSURE_COMP WAVE_HEIGHT REFRACT_STRENGTH
